[project]
name = "lmao-server"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "granian>=2.7.5",
    "gunicorn>=26.0.0",
    "uvicorn>=0.49.0",
]
